# yf-vitals

Unscoped alias of [`@yourfam/yf-vitals`](https://www.npmjs.com/package/@yourfam/yf-vitals). Same command (`yf-vitals`), same version.

```bash
npx yf-vitals
```

Install the scoped package if you can; this name exists so `npx yf-vitals` cannot be taken by someone else.
